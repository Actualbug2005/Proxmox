# nexus

Next.js app for the Proxmox management overlay. See the [repo-root README](../README.md) for architecture, configuration, and install instructions.

Dev:

```bash
npm install
npm run dev          # http://localhost:3000
npx tsc --noEmit     # typecheck
npm test             # node:test runner via tsx
```
